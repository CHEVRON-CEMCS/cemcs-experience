/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'links.papareact.com'], // Add the domain here
  },
};

export default nextConfig;
